<div id="lastest_posts" class="side_menu">
  <h4>Últimos Posts</h4>
  <ul>
    <% @posts.each do |post| %>
    <li><%= link_to post.title, permalink_url(post.permalink) %></li>
    <% end %>
  </ul>
  <span class="view_all"><%= link_to 'ver todos »', all_posts_url %></span>
</div>